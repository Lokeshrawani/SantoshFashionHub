name: SFH Admin Drag Drop Photos (manual legacy)

# Disabled automatic admin.html patching so it cannot create duplicate handlers.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Admin drag/drop patcher is disabled. The maintained Admin Photo Studio is the source of truth."