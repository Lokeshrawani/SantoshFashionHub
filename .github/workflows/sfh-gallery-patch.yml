name: SFH Gallery Patch (manual legacy)

# Disabled automatic repository mutation. Kept only as a manual historical workflow.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy gallery patch is disabled. The maintained storefront code is the source of truth."