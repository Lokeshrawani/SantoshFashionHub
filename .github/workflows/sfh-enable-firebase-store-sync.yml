name: Enable Firebase store sync (manual legacy)

# Disabled automatic repository mutation. Firebase bridge is maintained directly in source files.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Firebase store patcher is disabled."