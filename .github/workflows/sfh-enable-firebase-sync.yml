name: Enable SFH Firebase Store Sync (manual legacy)

# Disabled automatic repository mutation. The maintained bridge is committed directly.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Firebase sync injector is disabled."