name: SFH Photo Gallery Script (manual legacy)

# Disabled automatic patching so it cannot inject duplicate storefront photo code.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy photo gallery patcher is disabled. The storefront uses the maintained Firebase/portal bridge."