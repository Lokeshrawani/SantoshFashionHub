name: SFH Photo Enhancement (manual legacy)

# Disabled automatic patching so it cannot overwrite the live photo-fit styling.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy photo enhancer is disabled. Store photo styling is maintained in the live frontend code."